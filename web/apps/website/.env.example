# Website environment — Vite inlines VITE_* variables at build time (typed in src/env.d.ts).

# Origin of the backend API. Leave empty for same-origin relative requests —
# the dev server proxies /api -> http://localhost:5100 (vite.config.ts).
VITE_API_BASE_URL=
